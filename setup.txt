# self.projects.bit-craft-io.draft

wsl
# VSCode
Setting ›› Terminal › Integrated › Default Profile: Windows
change "Git Bash"

## setting "Git Bash for Windows"
vim ~/.bash_profile 

alias wsl='wsl -d Ubuntu -u guest'
PS1='\[\033[01;32m\]0026@win:\w\$ \[\033[00m\]'

. ~/.bash_profile

# GitHub
bit-craft-io
Psw@1***

# Outlook
bit.craft@outlook.com
Psw@1***

# wsl
cd ~ && ll
ln -s /mnt/d/self/projects/bit-craft-io/draft _draft
cd ~/_draft

## sudo no password
sudo visudo

# Add this below:
guest   ALL=(ALL)       NOPASSWD:ALL

ctrl+X, Enter

## ssh key make
mkdir -p ~/_draft/.ssh
ssh-keygen -t ed25519 -C "bit.craft@outlook.com" -f ~/_draft/.ssh/bit-craft-io

## ssh key copy
ll ~/.ssh/bit-craft-io
cp -a ~/_draft/.ssh/bit-craft-io ~/.ssh/bit-craft-io
chmod 600 ~/.ssh/bit-craft-io
ll ~/.ssh/bit-craft-io

## setting "config"
vim ~/.ssh/config

Host bc
  HostName github.com
  User git
  IdentityFile /mnt/d/self/projects/bit-craft/.ssh/bit-craft-io
  IdentitiesOnly yes

## git initialize
#git init
#git checkout -b main
#touch README.md
#git add README.md
#git commit -m "initial commit"
#git remote add origin git@bc:bit-craft-io/develop.git
#git push -u origin main

## git clone
cd /tmp
git clone git@bc:bit-craft-io/draft.git draft
sudo mv draft /var/www/

## git config in project
cd /var/www/draft
code .git/config

[user]
+	email = bit.craft@outlook.com
+	name  = bit-craft-io
[core]
+	autocrlf = input
+	eol = lf
